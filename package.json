{
  "name": "transparent-blog",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/public-site-react",
    "packages/contracts",
    "packages/design-system"
  ],
  "scripts": {
    "build": "node scripts/validate-content.mjs && npm run build --workspace @template/public-site-react && node scripts/render-static.mjs",
    "dev": "npm run dev --workspace @template/public-site-react",
    "test": "node --test tests/*.test.mjs"
  }
}
